services:
  pastebox:
    image: kmw0410/pastebox:latest
    container_name: pastebox
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DATA_DIR: "/paste-data"
      LISTEN_ADDR: ":8080"
      TZ: "Asia/Seoul"
    volumes:
      - ./data:/paste-data
